name: "Llama3Model"
pretrained_model_path: null # Path to pretrained model (if any)
params:
  trainer_name: "Llama3Trainer"
  type: "LLM" # Type of model (ML for machine learning, DL for deep learning)
  verbose: 2
preprocessing_ids:
  - "few_shot_paper_preprocessor" # Preprocessing ID for the model
  - "example_paper_preprocessor"
  # add more preprocessing IDs if needed
